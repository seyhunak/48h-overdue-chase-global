---
description: Implement the next planned task, test-first
---

Invoke the incremental-implementation and test-driven-development skills. Take the next unchecked task from tasks/plan.md + tasks/todo.md and only that one: failing test → pass → refactor → suite + `npx tsc --noEmit` → commit. Never bypass the ClearDue human approval gate. (Skills: `.agents/skills/<name>/SKILL.md`; speckit/`.specify` is retired.)
