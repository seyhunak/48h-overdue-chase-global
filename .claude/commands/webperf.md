---
description: Audit web performance — measure first, then optimize
---

Invoke the `performance-optimization` skill (`.agents/skills/performance-optimization/SKILL.md`; use `browser-testing-with-devtools` for real runtime data).

Measure Core Web Vitals / load before changing anything, profile to the bottleneck (frontend, backend, Convex queries), fix, then re-measure and report the delta.
